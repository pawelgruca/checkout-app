mutation checkoutComplete($checkoutId: ID) {
  checkoutComplete(checkoutId: $checkoutId) {
    order {
      id
      userEmail
      created
    }
    confirmationNeeded
    confirmationData
    errors {
      field
      message
      code
    }
  }
}
