mutation CheckoutPayemntCreate($checkouId: ID, $token: UUID, $input: PaymentInput!) {
  checkoutPaymentCreate(checkoutId: $checkouId, token: $token, input: $input) {
    checkout {
      id
    }
    payment {
      creditCard {
        brand
      }
    }
  }
}
