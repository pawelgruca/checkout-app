mutation CheckoutCreate {
  checkoutCreate(input: { channel: "channel-pln", email: "my1name@example.com", lines: [] }) {
    checkout {
      token
    }
    errors {
      field
      code
    }
  }
}
